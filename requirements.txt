# 1. 编译工具（适配 Python 3.10，避免编译失败）
Cython==3.0.10
setuptools==75.1.0  # 降低版本，避免与 PyYAML 编译冲突
wheel==0.44.0

# 2. 核心框架（适配 Python 3.10，且无二进制兼容问题）
tensorflow==2.18.0  # 支持 Python 3.10，且依赖 numpy 版本与 pandas 兼容
# tensorflow-macos==2.18.0  # 补充 Mac 本地兼容，云环境会自动忽略
numpy==1.26.4  # 满足 tensorflow 2.18.0 要求，且适配 pandas 1.5.3
pandas==1.5.3  # 与 matminer 0.9.0 兼容，且支持 Python 3.10
adapt==0.4.4  # 固定版本，避免自动升级冲突

# 3. 机器学习库（替换为 Python 3.10 适配版本，简化编译）
catboost==1.7.0  # 替换 1.2 版本，提供 Python 3.10 二进制包，无需复杂构建
xgboost==2.0.3  # 替换 1.7.4，适配 Python 3.10
scikit-learn==1.3.2  # 官方标准包名（连字符），适配 3.10
shap==0.45.1  # 适配 Python 3.10 和 numpy 1.26.4

# 4. 可视化与工具库（固定兼容版本）
altair==4.2.2
Bgolearn==2.0.0
graphviz==0.20.1
matplotlib==3.6.3
Pillow==9.5.0
PyYAML==6.0.2  # 替换 6.0，修复 Python 3.10 编译问题
requests==2.31.0
seaborn==0.12.2
sko==0.5.7
SciencePlots==2.1.0
prettytable==3.8.0
tabulate==0.9.0
pymoo==0.6.0.1
matminer==0.9.0
bayesian-optimization==1.4.3
pymatgen==2023.12.18

# 5. Streamlit 相关（确保与 Python 3.10 兼容）
streamlit==1.32.0  # 选择 3.10 适配的稳定版本，避免最新版冲突
streamlit-analytics==0.4.1
streamlit-extras==0.2.7
streamlit-option-menu==0.3.2
streamlit-shap==1.0.2

# 6. TensorFlow 依赖补充（避免云环境缺失）
tensorflow-estimator==2.18.0
tensorflow-io-gcs-filesystem==0.37.1